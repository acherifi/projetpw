import { Watchlist} from './objects/Watchlist';
export class WatchlistService {
  getWatchlistById(id: string): Watchlist {
    // TODO : requete à l'api
    return null;
  }
  addMovieToWatchlist(wl: Watchlist, movieId: string): boolean {
    // TODO : requete à l'api
    return null;
  }
  removeMovieFromWatchlist(wl: Watchlist, movieId: string): boolean {
    // TODO : requete à l'api
    return null;
  }
}
